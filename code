#!/bin/bash

# Navigate to the server directory
cd server/

# Accept the Minecraft EULA
echo "eula=true" > eula.txt

# Start the server (replace 'server.jar' with the actual server jar file name)
java -Xmx1G -Xms512M -jar server.jar nogui
